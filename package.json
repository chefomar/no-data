{
  "name": "tslint-no-data",
  "version": "1.3.0",
  "description": "TSLint rules to disallow the use of \"data\" as variable or function name",
  "scripts": {
    "build": "npm run clean:build && tsc -p ./tsconfig.json",
    "clean:build": "rimraf dist",
    "pretest": "npm run build",
    "test": "tslint --test ./src/test/**/tslint.json"
  },
  "keywords": [
    "tslint"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/chefomar/no-data.git"
  },
  "author": "omardev <elshinnaweyom@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.1",
    "rimraf": "^2.6.3",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "peerDependencies": {
    "tslint": "^5.0.0"
  }
}
